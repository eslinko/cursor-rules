═══════════════════════════════════════════════════════════════════════
  CLAUDE SETUP — Zeya888 Cursor Rules System
═══════════════════════════════════════════════════════════════════════

You are working with Zeya888 Cursor Rules System — a structured methodology
for AI-assisted development.

Throughout this conversation, you will receive different .mdc rule files
depending on the task type (testing, refactoring, analysis, etc).

═══════════════════════════════════════════════════════════════════════
  CRITICAL: PERSISTENT INSTRUCTION (Keep This Active Entire Conversation)
═══════════════════════════════════════════════════════════════════════

CURSOR RULES SYNTAX ADAPTER — Load Once, Apply Always
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

I've uploaded cursor-syntax-adapter.mdc to explain Cursor-specific syntax.

TREAT THIS AS ENVIRONMENT VARIABLE — PERSIST ENTIRE CONVERSATION:

┌─────────────────────────────────────────────────────────────────────┐
│ WHENEVER YOU SEE .mdc FILE:                                         │
│                                                                     │
│ 1. Front Matter (---YAML---)                                        │
│    → Extract "description" (rule purpose)                           │
│    → IGNORE "globs" and "alwaysApply" (Cursor IDE only)            │
│                                                                     │
│ 2. Cross-References (`@rule-name#section`)                          │
│    → Parse: file-name + section-anchor                             │
│    → Check: Is file in conversation/Projects?                      │
│    → If YES: Find "## #section", use content                       │
│    → If NO: Ask user OR note limitation                            │
│                                                                     │
│ 3. Anchor Sections (## #anchor)                                     │
│    → Mark as navigable section                                     │
│    → Target for cross-references from other rules                  │
│                                                                     │
│ 4. Auto-Triggers (AUTO-TRIGGERS @rule)                              │
│    → CANNOT auto-trigger (no event system)                         │
│    → INSTEAD: Remind user at checkpoint                            │
│    → Pattern: "⚠️ Checkpoint. Run @rule now?"                       │
│                                                                     │
│ 5. Modular Rules (.core.mdc)                                        │
│    → Filename has .core.mdc → Scan for appendix refs               │
│    → Extract all @rule.appendix.*.mdc references                    │
│    → If appendix NOT in context → Inform user (list files)         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

DO NOT FORGET THIS DURING CONVERSATION — APPLY TO EVERY .mdc FILE

═══════════════════════════════════════════════════════════════════════
  WORKING MODE
═══════════════════════════════════════════════════════════════════════

Task-Based Rule Loading:
  → Different tasks = different rules
  → I'll provide specific .mdc files as needed
  → You apply syntax adapter to each

Common Scenarios:

  📝 "Use @unit-test-build to create tests"
     → I upload unit-test-build.mdc
     → You apply adapter → extract front matter → follow phases
  
  🔍 "Use @analysis to document codebase"
     → I upload analysis.mdc
     → You apply adapter → follow "no assumptions" principle
  
  🎯 "Use @run-task to refactor feature"
     → I upload run-task.mdc
     → You apply adapter → ListX → ItemY decomposition

═══════════════════════════════════════════════════════════════════════
  YOUR RESPONSIBILITIES
═══════════════════════════════════════════════════════════════════════

✅ When I upload .mdc file:
   1. Apply syntax adapter (check front matter, detect modular, etc)
   2. If missing dependencies (@references) → inform me
   3. If AUTO-TRIGGER checkpoint → remind me
   4. Follow rule methodology as written

✅ When reach checkpoint (Gate 3, Phase completion, etc):
   1. Pause execution
   2. Show results
   3. If AUTO-TRIGGER specified → ask: "Run @next-rule now?"
   4. Wait my confirmation

✅ Throughout conversation:
   1. Keep syntax adapter active (don't forget!)
   2. Apply to EVERY .mdc file I provide
   3. Inform about missing dependencies proactively
   4. Follow Zeya888 methodology (ListX → ItemY, CR-loop, etc)

═══════════════════════════════════════════════════════════════════════
  CONFIRMATION
═══════════════════════════════════════════════════════════════════════

Please confirm you understand by responding:

"✅ Cursor Rules Adapter Loaded

I will:
- Apply syntax adapter to every .mdc file
- Extract front matter descriptions
- Check for cross-references and missing dependencies
- Detect modular rules (.core.mdc → list appendix)
- Remind you at AUTO-TRIGGER checkpoints
- Keep these instructions active entire conversation

Ready to receive first rule. What task are we working on?"

═══════════════════════════════════════════════════════════════════════

[Attach cursor-syntax-adapter.mdc to this conversation for reference]
[Then provide specific .mdc rule for your task]

